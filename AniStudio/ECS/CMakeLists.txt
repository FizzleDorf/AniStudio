# probably for the best we have a CMakeList in ECS to handle the includes
# We can use Global vars to dynamically include more comps and systems as needed
# entities are just vectors so they get no love (muhahaha)!